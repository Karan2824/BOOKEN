@page
@model BOOKEN.Pages.Books.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1 style="position:center; font-family:cursive;">Books Detail</h1>

<p>
    <a asp-page="Create" style="font-family:cursive;">Add New Book</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Book[0].Bookname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book[0].Plot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book[0].Releaseyear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Book[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Book) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bookname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Plot)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Releaseyear)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
